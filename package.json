{
  "name": "amplify-sveltekit-template",
  "version": "1.0.0",
  "scripts": {
    "build:app": "npm run build --workspace=packages/app",
    "preview:app": "npm run preview --workspace=packages/app"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "workspaces": "^0.1.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^1.0.0-next.44"
  }
}
